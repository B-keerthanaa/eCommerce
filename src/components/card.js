import React from "react";
import {Link} from "react-router-dom";

function Card(){
   
        return(
            <>
            
            {item.map((v)=>
                 <Link to="/electronics">Electronics</Link>
        )}      
                </>
        )
    }

export default Card;